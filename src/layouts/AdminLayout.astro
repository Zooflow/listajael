---
import Layout from './Layout.astro';
import '../styles/global.css';

interface Props {
  title: string;
}

const { title } = Astro.props;

---

<Layout title={`Admin - ${title}`}>
  <header class="bg-dark text-white p-4">
    <div class="container mx-auto flex justify-between items-center">
      <h1 class="text-2xl font-bold">Panel</h1>
      <nav>
        <ul class="flex space-x-4">
          <li><a href="/admin" class="hover:text-primary">Dashboard</a></li>
          <li><a href="/admin/likes" class="hover:text-primary">Cosas que me gustan</a></li>
          <li><a href="/admin/reminders" class="hover:text-primary">Cosas que me recuerdan</a></li>
          <li>
            <button id="logout-btn" class="bg-primary text-white px-3 py-1 rounded hover:bg-opacity-80">
              Cerrar sesión
            </button>
          </li>
        </ul>
      </nav>
    </div>
  </header>

  <main class="container mx-auto p-4 flex-grow">
    <slot />
  </main>

  <footer class="bg-dark text-white p-4 text-center">
    <p>No deberías estar aquí ( ｡ •̀ ᴖ •́ ｡)💢</p>
  </footer>

  <script>
    import { logout } from '../utils/authUtils';
    
    document.getElementById('logout-btn')?.addEventListener('click', () => {
      logout();
      window.location.href = '/';
    });
  </script>
</Layout>